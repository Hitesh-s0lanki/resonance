interface VoiceCreateDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function VoiceCreateDialog({ open: _, onOpenChange: __ }: VoiceCreateDialogProps) {
  return null;
}
